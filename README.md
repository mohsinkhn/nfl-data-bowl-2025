We start by looking at the data followed by a baseline approach.

We start by dashboard. Look at trajectories from a game as a trace.


In essence for each (game, play) -> we have a bunch of players moving in the direction of ball, we need predict trajectories of select few of them. We are given a landing location of ball so that helps a lot IMO.

Setup 1:
Since we have origin and target of the ball, we should 
